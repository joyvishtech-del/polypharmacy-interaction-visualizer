/**
 * Convenience re-export so consumers can import the hook from the
 * conventional `hooks/` location.
 */
export { useAuth } from '../context/AuthContext';
export type { AuthContextValue } from '../context/AuthContext';
